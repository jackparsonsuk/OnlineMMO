{
  "name": "onlinemmo",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "build:shared": "npm run build -w @mmo/shared",
    "dev": "npm run build:shared && concurrently -n server,client -c blue,green \"npm run dev -w @mmo/server\" \"npm run dev -w @mmo/client\"",
    "dev:server": "npm run dev -w @mmo/server",
    "dev:client": "npm run dev -w @mmo/client",
    "typecheck": "npm run build:shared && tsc -b packages/server packages/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.9.0"
  }
}
