node_modules
**/node_modules
**/dist
**/dist-types
data
.git
# Every package's, not just the root's: a stale build cache from this machine
# convinces `tsc -b` in the image that shared is already built, so it skips
# writing dist into the empty image and the server build finds no types.
**/*.tsbuildinfo
.env
